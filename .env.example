# Example environment file for Viht bot
# Copy this to .env on the server and fill in the real secrets/IDs.
# NEVER commit your real .env to the repository.

# Discord bot token (secret)
DISCORD_TOKEN=

# Discord application IDs
CLIENT_ID=
GUILD_ID=

# Channel IDs (use numeric channel IDs). Defaults are provided in code if empty.
WELCOME_CHANNEL_ID=
AI_CHAT_CHANNEL_ID=1437189999882801173
ANNOUNCE_CHANNEL_ID=1436487981723680930
MUSIC_LOG_CHANNEL_ID=

# Optional feature flags (set to 'true' to enable)
USE_MOCK_AI=false
GUILD_MEMBERS_INTENT=false
MESSAGE_CONTENT_INTENT=true

# AI provider key (Gemini / Google generative API key)
GEMINI_API_KEY=

# Role IDs used in reaction role handlers (example)
SUBSCRIBER_ROLE_ID=1441744621641400353

# If you supply ffmpeg via ffmpeg-static you don't need to set FFMPEG_PATH,
# the bot will set it automatically if ffmpeg-static is installed.

# Notes:
# - Create a file named .env on the server (not committed) with the actual values.
# - Keep tokens secret; do not paste them in public chat.
DISCORD_TOKEN=your_discord_token_here
CLIENT_ID=your_client_id_here
GUILD_ID=your_guild_id_here
GEMINI_API_KEY=your_gemini_api_key_here
AI_CHAT_CHANNEL_ID=test
PORT=3001
MESSAGE_CONTENT_INTENT=true
GUILD_MEMBERS_INTENT=true

# Supabase placeholders (if used locally)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Do NOT store real secrets in this file in the repo.
