const { EmbedBuilder, ActionRowBuilder, ButtonBuilder, ButtonStyle } = require('discord.js');

function createPriceMainEmbed() {
  return new EmbedBuilder()
    .setTitle('📜 Прайс — Информация об услугах')
    .setColor(0x5B9CFA)
    .setDescription('Приветствую, уважаемые подписчики! Мы помогаем автоматизировать рутинные задачи и развивать вашу инфраструктуру в интернете — сайты, боты и сложные веб‑сервисы. Ниже выберите категорию, чтобы получить подробную информацию.')
    .setFooter({ text: 'Для заказа — создайте тикет в службе поддержки.' });
}

function getMainRow() {
  return new ActionRowBuilder().addComponents(
    new ButtonBuilder().setCustomId('price_site').setLabel('🌐 Сайт').setStyle(ButtonStyle.Primary),
    new ButtonBuilder().setCustomId('price_bot').setLabel('🤖 Бот').setStyle(ButtonStyle.Success),
    new ButtonBuilder().setCustomId('price_other').setLabel('🛠️ Иное').setStyle(ButtonStyle.Secondary)
  );
}

function createSiteEmbed() {
  return new EmbedBuilder()
    .setTitle('🌐 Сайты и веб‑решения')
    .setColor(0x47D7AC)
    .setDescription([
      'Мы создаём профессиональные и автоматизированные веб‑решения под ключ.',
      'В пакет может входить: лендинг, многостраничный сайт, интернет‑магазин или многопользовательская платформа.',
      'Каждый проект получает админ‑панель для управления контентом и аналитики, а при необходимости мы интегрируем ИИ‑модули для автоматизации задач (модерация, генерация контента, интеллектуальные ответы).',
      'Чтобы обсудить требования и сроки — создайте тикет в службе поддержки: <#1442575929044897792>'
    ].join('\n\n'))
    .setFooter({ text: 'Нажмите «← Назад», чтобы вернуться к меню.' });
}

function createBotEmbed() {
  return new EmbedBuilder()
    .setTitle('🤖 Боты и автоматизация')
    .setColor(0x9F7AEA)
    .setDescription([
      'Разрабатываем ботов для Discord, Telegram, WhatsApp, веб‑приложений и других платформ.',
      'Возможности: обработка команд, работа с обращениями, интеграции с ML/AI для интеллектуальных ответов и автоматизации бизнес‑процессов.',
      'Примеры применения: служебные ассистенты, модерация, уведомления, интеграции с CRM и платёжными системами.',
      'Для заказа или уточнения требований создайте тикет в службе поддержки: <#1442575929044897792>'
    ].join('\n\n'))
    .setFooter({ text: 'Нажмите «← Назад», чтобы вернуться к меню.' });
}

function createOtherEmbed() {
  return new EmbedBuilder()
    .setTitle('🛠️ Другие сервисы')
    .setColor(0xFFC857)
    .setDescription([
      'Предоставляем комплексные облачные решения: VPN‑сервисы с панелью управления, игровые и прикладные серверы, интеграции с онлайн‑кассами и другими сервисами.',
      'Каждое решение проектируется под ваши требования и включает интерфейсы управления, пользовательские панели и автоматизацию бизнес‑логики.',
      'Для получения детального предложения и оценки сроков создайте тикет: <#1442575929044897792>'
    ].join('\n\n'))
    .setFooter({ text: 'Нажмите «← Назад», чтобы вернуться к меню.' });
}

function getBackRow() {
  return new ActionRowBuilder().addComponents(
    new ButtonBuilder().setCustomId('price_back').setLabel('← Назад').setStyle(ButtonStyle.Danger)
  );
}

module.exports = {
  createPriceMainEmbed,
  getMainRow,
  createSiteEmbed,
  createBotEmbed,
  createOtherEmbed,
  getBackRow
};
